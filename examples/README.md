# JYRY Command Center Examples

Examples and starter projects for JYRY Command Center (`jyry-command-center`).

- `minimal-command-center-project/` - minimal public bootstrap example that starts with `npm run bootstrap -- --project ./examples/minimal-command-center-project`, uses `docs/roadmap.md` as the tracker task source, and keeps JYRY-specific env optional
